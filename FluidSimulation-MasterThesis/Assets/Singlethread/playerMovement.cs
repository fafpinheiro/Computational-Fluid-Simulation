using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playerMovement : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject teste;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
      /*  if (Input.GetKey(KeyCode.A))
        {
            print(teste);
            teste.gameObject.GetComponent<teste>().objs.forcePhysic += new Vector3(10, 0, 0);
            print(teste);
        }
        if (Input.GetKey(KeyCode.D))
            teste.gameObject.GetComponent<teste>().objs.forcePhysic += new Vector3(-3, 0, 0);
        if (Input.GetKey(KeyCode.W))
            teste.gameObject.GetComponent<teste>().objs.forcePhysic += new Vector3(0, 0, 3);
        if (Input.GetKey(KeyCode.S))
            teste.gameObject.GetComponent<teste>().objs.forcePhysic += new Vector3(0, 0, -3);
        */

    }
}
